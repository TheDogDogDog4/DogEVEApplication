spring:
  application:
    name: elasticsearch-system

  elasticsearch:
    uris: http://elasticsearch:9200

  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
      config:
        server-addr: nacos:8848
        file-extension: yaml
        import-check:
          enabled: false
