spring:
  # Redis 配置
  data:
    redis:
      host: redis
      port: 6379
      password: 123456
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms